package objects;

import utils.Parser;
import java.io.File;

public class ArticleParser implements Parser {
    public ClassificationObject[] parseFile(File file) {
        return new ClassificationObject[1];
    }
}
